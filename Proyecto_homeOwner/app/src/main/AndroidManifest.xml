<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.homeowner.cesde">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".acutalizarUsuarioAnfitrionActivity"></activity>
        <activity android:name=".editarAptoActivity" />
        <activity android:name=".Usuario_invitado_Activity" />
        <activity android:name=".RegistroAptoActivity" />
        <activity android:name=".UsuarioActivity" />
        <activity android:name=".RegistroActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".screenSplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>